# API 连接面板拆分审查

## 变更范围

- 拆分 `app.py` 中侧边栏 API 连接面板。
- 新增小函数分别负责状态初始化、表单输入、连接校验、清除配置和状态展示。
- 保持原有 `test_deepseek_connection()`、`sync_session_api_config_to_env()` 和 Key 脱敏展示逻辑不变。

## 风险评估

- 风险等级：中低。
- 主要风险点：连接按钮与清除按钮的状态流转可能影响当前会话 API 配置。
- 已处理：拆分后仍在同一个 expander 内按原顺序执行，连接成功后继续同步环境变量，清除时继续恢复启动时环境变量。

## 回归建议

- 未填写 Key 时点击“连接 API”，应提示先填写 API Key。
- 填写错误 Key 时应显示连接失败，并且不显示“已连接”。
- 点击“清除配置”后，应恢复为未连接状态，并继续允许演示模式正常运行。
