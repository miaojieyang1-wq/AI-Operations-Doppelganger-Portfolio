# 用户洞察模式拆分审查

## 变更范围

- 拆分 `app.py` 中用户洞察模式的顶部说明、Tab 选择、访谈助手、问卷工坊、反馈清洗渲染逻辑。
- 保持原有按钮、输入框、任务标识、演示模式与 API 调用路径不变。
- 继续使用 `pending_*` 临时状态写入预设问题，避免在控件实例化后直接修改同名 `session_state`。

## 风险评估

- 风险等级：中低。
- 主要风险点：Tab 选择函数如果未返回当前选中项，会导致页面无法进入对应子功能。
- 已处理：`select_user_insight_tab()` 显式返回 `st.radio()` 的当前值，并通过语法检查验证。

## 回归建议

- 在用户洞察模式依次切换“访谈助手 / 问卷工坊 / 反馈清洗”。
- 点击访谈助手和问卷工坊的预设问题按钮，确认文本框可继续编辑。
- 在演示模式下分别生成访谈提纲、问卷和反馈清洗结果。
