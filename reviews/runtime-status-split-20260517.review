# 运行状态面板拆分审查

## 变更范围

- 拆分 `app.py` 中的 `render_runtime_status`。
- 新增小函数分别负责 API 状态格式化、调用来源展示、LangGraph 状态展示、知识库状态、侧边栏索引构建、异常展示和工作流图查看。
- 更新 `CHANGELOG.md`，记录本轮结构优化。

## 行为保持

- 保持原有运行状态展开面板标题和展示顺序不变。
- 保持 `sidebar_build_index` 按钮 key、索引构建流程和日志展示行为不变。
- 保持“查看工作流”按钮、Mermaid 优先和 ASCII 兜底逻辑不变。
- 保持最近异常展示和“当前没有异常”提示不变。

## 风险评估

- 风险等级：中低。
- 主要风险点：运行状态面板同时展示 API、LangGraph、RAG 和错误信息，拆分后容易遗漏某一段展示。
- 已处理：主函数仍按原顺序调用各子区块；索引构建和工作流图生成仅封装原有逻辑，不改变按钮触发条件。

## 回归建议

1. 打开侧边栏“运行状态”，确认调用方式、API 来源、模型、耗时、最近处理时间正常显示。
2. 触发一次生成后，确认最近识别意图和生成耗时更新。
3. 知识库未构建时，确认仍显示构建按钮和警告。
4. 点击“查看工作流”，确认可显示 Mermaid 或 ASCII 工作流文本。

## 已执行检查

- `.python-core/python.exe -m py_compile app.py`：通过
- `.python-core/python.exe -c "import app; ..."` 运行状态入口导入检查：通过
