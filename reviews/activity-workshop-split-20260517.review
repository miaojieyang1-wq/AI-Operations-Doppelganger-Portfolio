# 活动策划函数拆分审查

## 变更范围

- 拆分 `app.py` 中的 `render_activity_workshop`。
- 新增小函数分别负责顶部说明、活动输入、提示词读取、用户输入组装、活动方案生成、结果展示和 Markdown 导出。
- 更新 `CHANGELOG.md`，记录本轮结构优化。

## 行为保持

- 保持原有按钮文案、输入框 key、生成任务标识 `activity:workshop`、结果缓存字段和导出目录不变。
- 保持活动策划提示词继续优先从 `config/prompts/tasks/activity_workshop.yaml` 读取，缺失时使用代码兜底。
- 保持生成成功后记录运行状态、标记结果就绪并触发页面刷新。
- 保持导出时写入 `activity_plans/` 文件夹。

## 风险评估

- 风险等级：中低。
- 主要风险点：拆分生成流程后，运行耗时记录、错误提示或导出路径可能被遗漏。
- 已处理：生成函数内部仍统一负责成功/失败运行状态记录，导出函数只封装原有保存逻辑，不改变展示入口。

## 回归建议

1. 打开活动策划页，确认顶部说明、活动目标和活动背景输入框正常显示。
2. 未填写活动目标时点击“生成活动方案”，确认仍提示先输入活动目标。
3. 在演示模式或有效 API Key 下生成活动方案，确认结果、数据源声明和复制框正常显示。
4. 点击“导出为Markdown”，确认仍保存到 `activity_plans/` 并显示文件夹入口。

## 已执行检查

- `.python-core/python.exe -m py_compile app.py`：通过
- `.python-core/python.exe check_env.py`：通过
- `.python-core/python.exe smoke_test.py`：通过
- `.python-core/python.exe health_check.py`：通过
- `.python-core/python.exe -c "import app; ..."` 活动策划入口导入检查：通过

## 未执行项

- 当前沙箱策略拦截后台启动 Streamlit 服务，因此本轮未完成浏览器页面点击回归。
