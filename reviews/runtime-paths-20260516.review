# 审查报告：运行时数据目录兜底

## 审查范围

- `runtime_paths.py`
- `check_env.py`
- `README.md`
- `CHANGELOG.md`

## 结论

通过。修改属于小范围运行环境加固，不改变核心业务逻辑。

## 主要检查项

- 敏感信息未写入代码。
- 用户显式配置 `AI_OPS_DATA_DIR` 时不静默降级。
- 默认目录不可用时有备用目录。
- 环境检查脚本与运行时目录选择逻辑保持一致。
- 冒烟测试和健康检查通过。

## 剩余风险

- 如果系统临时目录也不可写，应用仍会启动失败。这属于环境权限问题，应由用户指定 `AI_OPS_DATA_DIR` 到可写目录。
- 临时目录作为兜底位置可能不适合长期保存报告和索引，后续可在 UI 中增加明显提示。
