# 决策看板拆分审查

## 变更范围

- 拆分默认首页 `render_decision_dashboard`。
- 将五阶段路线图数据提取为 `DECISION_DASHBOARD_STAGES`。
- 将标题、流程总览条、阶段卡片、展开工作台、功能入口和连接器拆成独立小函数。

## 风险评估

- 风险等级：中。
- 主要风险点：默认首页是用户进入应用后的第一屏，按钮跳转逻辑不能改变。
- 已处理：按钮仍调用原有 `switch_to_workspace()`，`target_insight_tab` 和 `target_version_tab` 的传递方式保持不变。

## 回归建议

- 打开默认首页，确认“运营路线图”五个阶段正常显示。
- 分别展开五个阶段，确认圆点高亮、说明文字和下一步提示正常。
- 点击每个阶段的入口按钮，确认能跳转到对应模式和 Tab。
