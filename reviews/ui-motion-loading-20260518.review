# UI 动效与加载状态优化审查

## 变更范围

- `style_assets.py` 新增独立 `MOTION_CSS` 样式层。
- `app.py` 新增 `render_processing_panel`，并接入主要生成流程。
- 更新 `CHANGELOG.md`，记录本轮 UI 体验优化。

## 优化内容

- 为模式卡、任务面板、结果卡、路线图卡片和按钮增加轻量入场与悬停反馈。
- 当前模式卡增加左侧高亮线和柔和脉冲，让功能切换更明确。
- 路线图激活连接线增加流动感，强化“运营流程推进”的视觉表达。
- 生成前展示统一的“正在编排/扫描/整理”状态条，减少只有按钮禁用和默认 spinner 的开发感。
- 主按钮统一调整为项目主色青绿色，替换 Streamlit 默认偏红主按钮。
- 增加 `prefers-reduced-motion`，降低动效对敏感用户的影响。

## 行为保持

- 不改变任何业务流程、按钮 key、输入输出字段或报告生成逻辑。
- 加载状态条只在提交后、实际生成前渲染，不参与模型调用参数和结果保存。
- CSS 动效为渐进增强，浏览器不支持时不影响功能可用性。

## 风险评估

- 风险等级：中低。
- 主要风险点：全局按钮样式可能影响主按钮视觉一致性。
- 已处理：通过本地浏览器检查确认侧边栏当前按钮和“生成回答”按钮均显示为项目主色；保留禁用态和悬停态样式。

## 已执行检查

- `.python-core/python.exe -m py_compile app.py style_assets.py`：通过
- `.python-core/python.exe health_check.py`：通过
- `.python-core/python.exe smoke_test.py`：通过
- 本地 Streamlit 页面浏览器检查：首页、侧边栏、关于我模式、当前模式卡和主按钮样式正常。
