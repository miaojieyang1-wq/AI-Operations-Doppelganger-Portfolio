# API 超时边界加固审查

## 修改范围

- `api_runtime.py`
- `config_loader.py`
- `config/app.yaml`
- `CHANGELOG.md`

## 修改内容

1. 在默认配置中新增 `connect_timeout_seconds`、`read_timeout_seconds`、`max_retries`。
2. 在 `config/app.yaml` 中同步新增相同配置项。
3. `api_runtime.py` 创建 OpenAI 兼容客户端时统一设置连接超时、读取超时和最大重试次数。
4. 连接测试和正式模型调用共用同一套超时边界。

## 影响评估

- 不改变任何前端入口和业务输出结构。
- 不改变模型名、接口地址、温度等既有配置。
- 模型服务无响应时会更快失败，避免页面长时间卡住。
- 如果网络环境很慢，可能比过去更早报错，但会让错误更可控。

## 回归测试建议

1. 演示模式下生成一次“了解我”回答。
2. 使用有效 API Key 点击“连接 API”，确认连接测试能成功。
3. 使用无效 API Key 点击“连接 API”，确认页面提示失败。
4. 生成一次问卷或访谈，确认可正常进入结果或明确提示 API 问题。
