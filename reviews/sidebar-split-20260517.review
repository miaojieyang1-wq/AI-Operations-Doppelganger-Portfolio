# 侧边栏导航拆分审查

## 变更范围

- 拆分 `app.py` 中的 `render_sidebar`。
- 新增小函数分别负责侧边栏品牌区、演示模式开关、默认模式初始化、导航按钮、主模式到工作流映射和工作流说明。
- 更新 `CHANGELOG.md`，记录本轮结构优化。

## 行为保持

- 保持原有侧边栏按钮文案、展示顺序和 `main_mode` 写入行为不变。
- 保持默认进入 `dashboard` 的初始化逻辑不变。
- 保持演示模式 `AI_OPS_DEMO_MODE` 环境变量同步和说明文案不变。
- 保持工作流说明和运行状态面板入口不变。

## 风险评估

- 风险等级：中低。
- 主要风险点：主模式到工作流 key 的映射如果写错，会导致点击侧边栏后进入错误页面。
- 已处理：拆分后保留原映射关系；`work` 和其他未知模式继续映射到 `version`。

## 回归建议

1. 依次点击侧边栏六个入口，确认能进入对应页面。
2. 打开和关闭演示模式，确认提示文案和生成模式同步变化。
3. 确认默认首次进入仍停留在“运营决策中心”。
4. 确认侧边栏底部仍显示当前模式说明和运行状态面板。

## 已执行检查

- `.python-core/python.exe -m py_compile app.py`：通过
- `.python-core/python.exe -c "import app; ..."` 侧边栏入口与主模式映射检查：通过
