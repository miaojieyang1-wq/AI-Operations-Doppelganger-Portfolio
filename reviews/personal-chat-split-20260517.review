# 关于我问答拆分审查

## 变更范围

- 拆分 `app.py` 中的 `render_personal_chat_form`。
- 拆分 `run_personal_chat` 中的结果持久化、本地兜底和 Agent 生成流程。
- 更新 `CHANGELOG.md`，记录本轮结构优化。

## 行为保持

- 保持原有常见问题预设、文本框 key、预设问题写入 `pending_personal_question` 和自动提交标记不变。
- 保持聊天历史展示、`personal_messages` 和 `personal_display_messages` 的记录行为不变。
- 保持 LangGraph 优先、DeepSeek 直连兜底、本地兜底的调用顺序不变。
- 保持生成结果写入 `last_report`、`last_workflow`、`last_report_path` 和结果定位逻辑不变。

## 风险评估

- 风险等级：中。
- 主要风险点：关于我模式依赖多轮历史和预设问题自动提交，拆分时容易破坏输入框接管或历史记录。
- 已处理：预设问题状态同步仍发生在文本框渲染前；结果持久化仍统一调用原有历史记录函数。

## 回归建议

1. 点击“关于我”，确认聊天历史和问题输入框正常显示。
2. 点击任意常见问题，确认问题会写入输入框并自动提交。
3. 连续提问两轮，确认回答能保留上下文。
4. 无 API Key 或演示模式下，确认仍能返回本地兜底/演示回答。

## 已执行检查

- `.python-core/python.exe -m py_compile app.py`：通过
- `.python-core/python.exe -c "import app; ..."` 关于我问答入口与预设问题检查：通过
