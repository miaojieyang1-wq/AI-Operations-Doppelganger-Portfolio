# 管理后台拆分审查

## 变更范围

- 拆分 `app.py` 中的 `render_admin_mode`。
- 新增小函数分别负责后台初始化、后台登录、本地报告清理、数据保留策略、报告统计、知识库重建、知识库管理、配置检查器、缓存清理和后台仪表盘组装。
- 更新 `CHANGELOG.md`，记录本轮结构优化。

## 行为保持

- 保持后台初始化、登录和演示登录按钮 key 不变。
- 保持密码长度校验、确认密码校验和密码哈希保存逻辑不变。
- 保持本地报告清理仅允许清理 `runtime_paths.USER_DATA_DIR` 下报告目录的保护条件不变。
- 保持知识库重建按钮、构建日志展示和失败提示不变。
- 保持配置检查器展示 YAML 内容、缺失配置提示和缓存清理字段不变。

## 风险评估

- 风险等级：中。
- 主要风险点：管理后台包含本地文件清理和索引重建动作，拆分时不能扩大清理范围或改变按钮触发条件。
- 已处理：清理函数仍保留 `runtime_paths.USER_DATA_DIR.resolve() in resolved_folder.parents` 保护条件；索引重建和缓存清理仅封装原有逻辑。

## 回归建议

1. 未初始化后台时，确认仍显示密码设置区并执行长度与二次确认校验。
2. 已初始化但未解锁时，确认密码登录和演示登录入口仍可用。
3. 解锁后确认系统信息、数据保留策略、报告统计、知识库管理、配置检查器和缓存管理均显示。
4. 点击“清除对话缓存”，确认只清理既定 session_state 字段。
5. 点击“重新构建索引”，确认日志和成功/失败提示仍正常。

## 已执行检查

- `.python-core/python.exe -m py_compile app.py`：通过
- `.python-core/python.exe -c "import app; ..."` 管理后台入口导入检查：通过
- `Select-String` 检查本地报告清理目录保护条件：通过
